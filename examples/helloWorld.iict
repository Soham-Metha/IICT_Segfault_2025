x:str = "Hi";

main:func() = {

     y:i64 = 0;
     y < 10 <-> {
          write("hello,world!\n");
          y = y + 1;
     }
     x = "Hi";
     % TODO: control directly jumps to main
     % without defining the global variables
     write(x);
     % write(y); not yet supported
     
     (false) repeat {
          write("bye,world!\n");
     }

    x = "hello,world!";
    write(x);

    1;
};

% Language name: undecided
% Created as a part of the 
% IICT SEGFAULT 2025 hackathon
% by Team Kernull

% Grammar:
% CodeBlock // Can be called program modules
%  |-> StmtNode* begin
%  |-> StmtNode* end
%
% StmtNode:
%  |-> Stmt
%  |-> StmtNode* next

% Currently supported statements:
% Variable declaration:
% <var> : <type>;
% Variable definition:
% <var> : <type> = Stmt;